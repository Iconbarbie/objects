// create an object called my credentials
//with keys name,city and address

let myCredentials = {
    name: "Jefrey Way",
    city: "Miami",
    address: "456th street",
};
console.log(
    `My name is ${myCredentials.name} and my street address is ${myCredentials.address} in downtown
     ${myCredentials.city}`

);